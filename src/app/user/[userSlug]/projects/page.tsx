import React from 'react'
import { Button } from '@/components/ui/button'

type Props = {}

const userProjectsPage = (props: Props) => {
  return (
    <>
      <Button>
        Create a New Project
      </Button>
    </>
  )
}

export default userProjectsPage
